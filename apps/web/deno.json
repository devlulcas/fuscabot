{
  "imports": {
    "@hono/hono": "jsr:@hono/hono@^4.8.3",
    "@std/assert": "jsr:@std/assert@^1.0.13"
  },
  "tasks": {
    "check": "deno fmt --check && deno lint && deno check mod.ts src/app_test.ts && deno test",
    "test": "deno test"
  },
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "@hono/hono/jsx"
  },
  "fmt": { "lineWidth": 100 },
  "lint": { "rules": { "tags": ["recommended"] } }
}
